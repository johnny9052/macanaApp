import { IndicadorDecimalPipe } from './indicador-decimal.pipe';

describe('IndicadorDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new IndicadorDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
