/* Modelo del objeto permisos*/
export interface ModelPermisos {
    nombre: string;
    codigo: string;
    padre: string;
    descripcion: string;
    prioridad: string;
    icono: string;
    activo: boolean;
  }
