/* Modelo del objeto permisos*/
export interface ModelPermisos {
    id: string;
    nombre: string;
    codigo: string;
    codpadre: string;
    nombrepadre: string;
    prioridad: string;
    icono: string;
    activo: boolean;
    descripcion: string;
  }
